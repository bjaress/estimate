module ProgramInfo where

version = "1.1.0"

namespace = "bjaress"
name = "estimate"

fullName = namespace ++ "/" ++ name
url = "https://github.com/" ++ fullName

fullVersion = fullName ++ ":" ++ version
